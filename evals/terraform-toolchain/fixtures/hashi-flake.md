# Hashi Template Starting Point

The repository usually starts with:

```bash
nix flake init -t github:the-nix-way/dev-templates#hashi
```

Then the project adds Terraform/OpenTofu-specific tools to the generated devShell.
